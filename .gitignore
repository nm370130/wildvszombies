# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/
*.egg

# Virtual environments
.venv/
venv/
env/
ENV/

# Debug / smoke test scripts (not production)
math/test_*.py

# Simulation output artifacts (generated by run.sh — not source code)
math/library/
math/output/
math/books/
math/*.json
math/*.csv.bak
math/*.log

# macOS
.DS_Store

# IDE (contains machine-specific paths — each dev sets their own)
.idea/
.vscode/
*.swp
*.swo

# cProfile dumps
math/*.prof

# Claude Code session memory (machine-specific, not source code)
.claude/

# Internal docs — local only, not for the repo
CLIENT_UPDATE.md
DEPLOY.md
FRONTEND_HANDOFF.md
IMPLEMENTATION_PLAN.md
PAYTABLE_UPDATE.md
PRODUCT_TEAM_QUESTIONS.md

# Math SDK source files (belong in math-sdk repo, accessed via PYTHONPATH)
src/
